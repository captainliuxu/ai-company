# AI Companion Pro — 功能验收清单

> 按 Phase 1→8 逐条列出所有已实现功能。请在验收通过的项目打 ✅，不通过打 ❌，并可在备注栏加批注。

---

## Phase 1: Persona System（角色系统）

### 1.1 数据模型

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 1 | Persona ORM 模型 — 7 个字段（id/name/personality/speaking_style/background_story/emotional_traits/avatar_url/created_at） | Swagger 返回数据检查 | ⬜ | |
| 2 | emotional_traits 为 JSON 类型存储 | 数据库查看或 API 返回 dict | ⬜ | |
| 3 | created_at 使用 timezone.utc（非弃用的 utcnow） | 启动无弃用警告 | ⬜ | |

### 1.2 种子数据

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 4 | 首次启动自动插入 3 个角色（小暖/小锐/小默） | 首次启动后 GET /personas | ⬜ | |
| 5 | 再次启动不重复插入 | 重启后 GET /personas 仍返回 3 条 | ⬜ | |
| 6 | 小暖：温柔体贴、软糯温和风格 | 查看角色详情 | ⬜ | |
| 7 | 小锐：理性毒舌、直接利落风格 | 查看角色详情 | ⬜ | |
| 8 | 小默：安静沉稳、简洁风格 | 查看角色详情 | ⬜ | |

### 1.3 Persona API

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 9 | `GET /api/v1/personas` 返回 200 + 3 个角色 | 浏览器 / Swagger | ⬜ | |
| 10 | `GET /api/v1/personas/{valid_id}` 返回角色详情 | Swagger 输入有效 ID | ⬜ | |
| 11 | `GET /api/v1/personas/{invalid_id}` 返回 404 | Swagger 输入无效 ID | ⬜ | |
| 12 | 响应格式统一 `{"success": bool, "message": str, "data": ...}` | 查看 JSON 结构 | ⬜ | |

---

## Phase 2: Chat API + Prompt Builder

### 2.1 Chat API

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 13 | `POST /api/v1/chat/session` 创建会话，返回 session_id | Swagger 测试 | ⬜ | |
| 14 | 不存在的 persona_id 创建会话返回 404 | Swagger 输入无效 ID | ⬜ | |
| 15 | `POST /api/v1/chat/send` SSE 流式返回，逐 token 推送 | 前端发送消息观察 | ⬜ | |
| 16 | 不存在的 session_id 发送消息返回 404 | Swagger 测试 | ⬜ | |
| 17 | AI API 返回 HTTP 错误时，SSE 返回 error 事件（不崩溃） | 模拟错误场景 | ⬜ | |

### 2.2 Prompt Pipeline

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 18 | system prompt 包含完整 Persona Block（性格/说话风格/背景/情绪特质） | 抓包或日志查看 | ⬜ | |
| 19 | 不同角色回复风格明显不同（小暖温柔 vs 小锐直接 vs 小默简洁） | 分别选 3 个角色对话 | ⬜ | |
| 20 | system prompt 作为顶层 `system` 参数发送，不在 messages 数组中 | 后端日志/抓包 | ⬜ | |

---

## Phase 3: Emotion System（情绪系统）

### 3.1 情绪分析

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 21 | 每次对话后自动分析情绪（好感度/信任度/心情/依赖度） | 发消息后查情绪 API | ⬜ | |
| 22 | 情绪分析失败不阻塞聊天（fallback 到默认值 50/50/平静/50） | 模拟 AI 错误 | ⬜ | |

### 3.2 情绪 API

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 23 | `GET /api/v1/emotion/{session_id}` 返回当前情绪四维度 | Swagger | ⬜ | |
| 24 | `GET /api/v1/emotion/{session_id}/history` 返回情绪变化时间线 | Swagger | ⬜ | |
| 25 | 不存在的 session_id 返回 404 | Swagger | ⬜ | |
| 26 | 前端聊天页顶部实时展示情绪四维度面板 | 前端发消息后观察 | ⬜ | |

### 3.3 Emotion Prompt Block

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 27 | system prompt 中追加情绪状态信息（好感/信任/心情/依赖） | 后端日志查看 | ⬜ | |
| 28 | 不同情绪状态下 AI 回复语气有差异 | 多轮对话观察 | ⬜ | |

---

## Phase 4: Long-term Memory（长期记忆）

### 4.1 记忆提取

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 29 | 对话后自动 AI 提取记忆（user_info / preference / event / emotion） | 发消息后查记忆 API | ⬜ | |
| 30 | 记忆包含 importance 评分（1-5） | 查看记忆数据 | ⬜ | |
| 31 | 记忆自动持久化到 SQLite（重启不丢失） | 重启后端后查记忆 | ⬜ | |

### 4.2 记忆 API

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 32 | `GET /api/v1/memories/{session_id}` 返回记忆列表 | Swagger | ⬜ | |
| 33 | `?type=preference` 过滤正常 | Swagger | ⬜ | |
| 34 | `?type=user_info` 过滤正常 | Swagger | ⬜ | |

---

## Phase 5: Lightweight RAG（语义检索）

### 5.1 Embedding

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 35 | sentence-transformers (all-MiniLM-L6-v2) 加载成功 | 启动日志查看 | ⬜ | |
| 36 | 新记忆存储时自动生成 384 维 embedding | 数据库查看 embedding 字段 | ⬜ | |
| 37 | 模型加载失败不阻塞聊天（RAG 降级跳过） | 模拟模型缺失 | ⬜ | |

### 5.2 语义搜索

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 38 | 发送消息时自动用用户消息搜索相关记忆（top_k=5） | 后端日志 | ⬜ | |
| 39 | 相似度 < 0.3 的记忆被过滤 | 后端日志/抓包 | ⬜ | |
| 40 | 无 embedding 时降级为关键词匹配 | 模拟 | ⬜ | |

### 5.3 Memory Prompt Block

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 41 | 召回的相关记忆注入 system prompt | 后端日志查看 prompt | ⬜ | |
| 42 | AI 回复能自然融入记忆信息（不刻意列举） | 对话中提之前聊过的内容 | ⬜ | |

---

## Phase 6: Conversation Summary（对话摘要）

### 6.1 摘要触发

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 43 | 对话满 20 轮（用户消息 ≥20 条）自动触发摘要 | 连续发 20 条消息 | ⬜ | |
| 44 | 摘要内容覆盖用户近况、情绪变化、关系变化、重要事件 | 查看摘要内容 | ⬜ | |
| 45 | 摘要存入 memory（type="summary"） | 查记忆 API | ⬜ | |

### 6.2 对话压缩

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 46 | 摘要后旧消息被压缩为系统消息，只保留最后 5 条 | 多轮对话后观察连贯性 | ⬜ | |
| 47 | 摘要后对话仍保持连贯 | 摘要后继续对话 | ⬜ | |

---

## Phase 7: Product Chat UI（聊天前端）

### 7.1 角色选择页 `/personas`

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 48 | 页面展示 3 张角色卡片（小暖/小锐/小默） | 访问 localhost:3000/personas | ⬜ | |
| 49 | 每张卡片显示名字 + 性格描述 + 说话风格 | 目视检查 | ⬜ | |
| 50 | 点击角色跳转到 `/chat?persona_id={id}` | 点击卡片 | ⬜ | |
| 51 | API 加载失败显示错误提示 | 停止后端后刷新 | ⬜ | |
| 52 | 加载中显示 spinner 动画 | 刷新页面观察 | ⬜ | |
| 53 | 首页 `/` 自动重定向到 `/personas` | 访问 localhost:3000 | ⬜ | |

### 7.2 聊天页 `/chat`

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 54 | 页面顶部显示角色名字 + 性格描述 | 选角色后进入聊天 | ⬜ | |
| 55 | 用户消息右对齐（橙色气泡） | 发送消息 | ⬜ | |
| 56 | AI 消息左对齐（灰色气泡） | 收到回复 | ⬜ | |
| 57 | 消息列表自动滚动到底部 | 发多条消息 | ⬜ | |

### 7.3 SSE 流式交互

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 58 | AI 回复逐字显示（无卡顿） | 发送消息观察 | ⬜ | |
| 59 | 等待回复时显示三点跳动动画（typing indicator） | 发消息后立即观察 | ⬜ | |
| 60 | 回复中显示闪烁光标 | 观察 | ⬜ | |
| 61 | 发送中禁用输入框和发送按钮 | 观察 | ⬜ | |
| 62 | Enter 键发送消息 | 键盘操作 | ⬜ | |
| 63 | SSE 连接中断显示错误提示 | 模拟中断 | ⬜ | |

### 7.4 情绪面板

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 64 | 聊天页顶部显示四维度（粉色好感/蓝色信任/橙色心情/紫色依赖） | 发消息后观察 | ⬜ | |
| 65 | 每次回复后自动刷新情绪数据 | 发多条消息观察变化 | ⬜ | |

### 7.5 错误与边界

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 66 | 无 persona_id 访问 /chat 时显示"缺少角色ID" + 返回链接 | 直接访问 /chat | ⬜ | |
| 67 | 角色不存在时显示"角色不存在" + 返回链接 | /chat?persona_id=invalid | ⬜ | |
| 68 | API 错误时显示红色错误横幅，可关闭 | 停止后端后发消息 | ⬜ | |
| 69 | 并发发送请求时，前一个 SSE 被 abort | 快速连发两条消息 | ⬜ | |

### 7.6 响应式与视觉

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 70 | 移动端（<768px）全屏聊天布局 | 浏览器缩小窗口 | ⬜ | |
| 71 | 桌面端居中 max-w-2xl | 宽屏查看 | ⬜ | |
| 72 | 暖色调主题（橙/奶油/浅棕渐变背景） | 目视 | ⬜ | |
| 73 | 无消息时显示问候语（"向xxx发送第一条消息吧~"） | 新建会话进入聊天 | ⬜ | |
| 74 | 页面标题 "AI Companion Pro" | 浏览器标签页 | ⬜ | |

---

## Phase 8: 集成调试 + Demo 打磨

### 8.1 全链路

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 75 | 启动后端 → 角色列表 API 返回 3 个角色 | 完整走一遍 | ⬜ | |
| 76 | 前端选"小暖" → 创建 session → 发送消息 → SSE 流式显示温柔语气回复 | 完整走一遍 | ⬜ | |
| 77 | 切换"小锐" → 回复风格变直接利落 | 新建会话选小锐 | ⬜ | |
| 78 | 多轮对话 → 情绪状态变化 → 前端情绪面板更新 | 发 5+ 条消息 | ⬜ | |
| 79 | 对话中提及个人信息 → Memory API 可查到 | 说"我叫张三，喜欢打篮球"后查 API | ⬜ | |
| 80 | 再次发送消息 → RAG 召回相关记忆 | 发相关消息观察回复 | ⬜ | |
| 81 | 20+ 轮对话 → 自动触发摘要 | 发 20 条消息 | ⬜ | |

### 8.2 边界情况

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 82 | 空消息发送被前端拦截（按钮禁用） | 不输入文字点发送 | ⬜ | |
| 83 | AI API 超时 → 前端不崩溃，有错误提示 | 模拟（或等超时） | ⬜ | |
| 84 | 数据库文件 `data/app.db` 自动创建 | 检查 data 目录 | ⬜ | |
| 85 | 后端启动日志无错误 | 查看终端输出 | ⬜ | |
| 86 | CORS 同时支持 localhost:3000 和 127.0.0.1:3000 | 两种地址都试试 | ⬜ | |

### 8.3 健壮性

| # | 验收项 | 验证方式 | 通过? | 备注 |
|---|--------|---------|:-----:|------|
| 87 | EmotionService 初始化失败不崩 /chat/send | 模拟（DB 断开等） | ⬜ | |
| 88 | MemoryService/RAG 初始化失败不崩 /chat/send | 模拟模型缺失 | ⬜ | |
| 89 | RAG 搜索失败降级为关键词匹配 | 模拟 | ⬜ | |
| 90 | 前端组件卸载时正确 cleanup（无内存泄漏警告） | 浏览器 console | ⬜ | |

---

## 验收统计

| 类别 | 总数 | 通过 | 未通过 |
|------|:---:|:----:|:-----:|
| Phase 1: Persona System | 12 | 0 | 0 |
| Phase 2: Chat API + Prompt | 8 | 0 | 0 |
| Phase 3: Emotion System | 8 | 0 | 0 |
| Phase 4: Long-term Memory | 6 | 0 | 0 |
| Phase 5: Lightweight RAG | 8 | 0 | 0 |
| Phase 6: Summary | 5 | 0 | 0 |
| Phase 7: Chat UI | 27 | 0 | 0 |
| Phase 8: Integration | 16 | 0 | 0 |
| **总计** | **90** | **0** | **0** |

---

## 批注汇总

> 用户在此处写全局性的反馈，或针对特定验收项的批注。

```

